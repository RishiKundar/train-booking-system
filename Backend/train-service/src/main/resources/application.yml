server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mydb?currentSchema=train_service
    username: app_project
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true